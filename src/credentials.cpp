#include "credentials.h"

// WiFi Configuration ONLY for simple stable connection
const char* WIFI_SSID = "TNCAP7550FE 2.4";
const char* WIFI_PASSWORD = "906425FD47";

// --- OTA (Over-the-Air) Update Settings ---
// Hostname for the robot on the network (e.g., "wheelie-robot.local")
const char* OTA_HOSTNAME = "wheelie-robot";
// Password - MUST match the 'upload_flags' in platformio.ini
const char* OTA_PASSWORD = "080493";
